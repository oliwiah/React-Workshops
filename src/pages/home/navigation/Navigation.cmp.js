import React from 'react';

import {
  NavigationContainer,
  NavTitle,
} from './Navigation.styles';

const Navigation = props => (
    <NavigationContainer>
        <NavTitle>My Application</NavTitle>
    </NavigationContainer>
);

export default Navigation;
