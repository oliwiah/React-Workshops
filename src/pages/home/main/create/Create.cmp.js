import React from 'react';

import {
    CreateContainer,
    ActionContainer,
    Button,
    Input
} from './Create.styles';

const Create = (props) => (
    <CreateContainer>
        TBC
    </CreateContainer>
);

export default Create;